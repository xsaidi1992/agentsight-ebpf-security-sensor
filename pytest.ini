[pytest]
testpaths = tests
markers =
    kernel: requires Linux BTF, libbpf, tracepoints and BPF capabilities
    e2e: end-to-end kernel-to-API/session integration
    build: requires the complete native eBPF build toolchain
addopts = -ra
